[package]
name = "stellar-swap-shared"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { version = "26.0.1" }

[dev-dependencies]
soroban-sdk = { version = "26.0.1", features = ["testutils"] }
